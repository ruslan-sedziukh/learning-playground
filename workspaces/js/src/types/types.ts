/**
 * Represents an interval with a start and end time.
 *
 * @typedef Interval
 * @type {[number, number]}
 * @property {number} [0] - The start time of the interval.
 * @property {number} [1] - The end time of the interval.
 */
export type Interval = [number, number]
